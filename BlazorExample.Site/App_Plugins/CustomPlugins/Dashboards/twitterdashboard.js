angular.module("umbraco")
    .controller("TwitterDashboard", function ($scope, $location, userService, assetsService, $http, $q, notificationsService) {
    
    var vm = this;
    vm.Loading = false;
    
});