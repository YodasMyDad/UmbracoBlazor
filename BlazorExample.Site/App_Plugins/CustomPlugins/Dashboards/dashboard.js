angular.module("umbraco")
    .controller("UtilityDashboard", function ($scope, $location, userService, assetsService, $http, $q, notificationsService) {
    
    var vm = this;
    vm.Loading = false;
    
});